assembly-versioning-scheme: MajorMinorPatch
# v6 renamed the deployment modes: this "ContinuousDelivery" is what v5 called
# "ContinuousDeployment" (prerelease that increments per commit).
mode: ContinuousDelivery
branches:
  main:
    regex: ^main$|^master$
    label: ci
ignore:
  sha: []
